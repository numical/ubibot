// the public api
const checkBot = require("./checkBot");
const createContentTemplate = require("./createContentTemplate");
const prefixes = require("./prefixes");

module.exports = { checkBot, createContentTemplate, prefixes };
